
version: "3.9"

services:
  server:
    build: ./server
    env_file: ./server/.env
    ports:
      - "8080:8080"

  client:
    build: ./client
    environment:
      VITE_API_BASE: "http://localhost:8080"

      # IDs fixos (seu workspace e relatório)
      VITE_WORKSPACE_ID: "108148f0-f6cc-424f-b53d-f3532203cda1"
      VITE_REPORT_ID:    "687d3a0e-5dbc-44ce-9338-371f2df4a6c2"

      # Informe a origem do filtro "Empresa"
      # (ex.: DimEmpresa / NomeEmpresa)
      VITE_EMPRESA_TABLE:  ""
      VITE_EMPRESA_COLUMN: ""

    ports:
      - "3000:80"
    depends_on:
      - server
